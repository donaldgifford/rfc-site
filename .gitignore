node_modules
dist
build
.react-router
coverage

# Build / tool caches
.tsbuildinfo
*.tsbuildinfo
.eslintcache

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea

# Env
.env
.env.local
.env.*.local

# Claude Code loop state (per-session, not for VCS)
.claude/donald-loop.local.md

# Local-only personal trackers / notes — mirrors the mise.local.toml /
# .env.*.local convention. Use *.local.md at the repo root for sticky
# notes that should not leave the working copy.
*.local.md

# Historical CLAUDE.md backed up before the RFC-0001 / IMPL-0005 rewrite.
# Git history has it too (last live revision: 4c6bc9b); the .bak is for
# offline reference while the rebuild is in progress.
CLAUDE.md.*.bak

# mise per-user overrides (mise.toml is committed; mise.local.toml is not)
mise.local.toml

# Generated TS client from api/openapi.yaml (orval output).
# Per ADR-0001: drift = CI failure; output is never committed.
src/portal/api/__generated__/
