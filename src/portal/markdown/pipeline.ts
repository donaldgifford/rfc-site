// Phase 1 stub. Phase 2 wires the unified plugin chain
// (parse → gfm → strip-docz-boilerplate → rehype → slug → autolink →
//  shiki → mermaid-marker → sanitize) as a module-level singleton.
// See docs/impl/0003-wire-up-the-markdown-rendering-pipeline-per-design-0002.md.
export {};
