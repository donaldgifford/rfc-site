// Phase 1 stub. Phase 4 implements the `<pre>` / `<code>` wrapper:
//   - Pass through Shiki-highlighted code blocks unchanged.
//   - Detect `data-mermaid-source` (set by the mermaid-marker rehype plugin)
//     and route the block to `<MermaidBlock>` for client-side hydration.
//   - Leave the `<pre>` shape ready for a future copy button.
export {};
